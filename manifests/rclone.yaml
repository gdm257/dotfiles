entrypoint: rclone copy
command: >-
  --progress -v
  --immutable
  --ignore-existing
  --size-only --ignore-checksum
  --ignore-times
  --filter "- desktop.ini"
  --fast-list

run:
  - args: >-
      'remote-1:/src/foo/'
      'remote-2:/dst/foo/'
